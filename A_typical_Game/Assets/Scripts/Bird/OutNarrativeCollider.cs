using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OutNarrativeCollider : MonoBehaviour {

    //The part bellow can't work because the colliders block from teleporting
    /* public bool PassNarrativeArea;

     void OnTriggerEnter(Collider c)
     {
         //If the player is going forward in the game and not going to the narrative elements
         if (c.GetComponent<Camera>())
         {
             PassNarrativeArea = true;
         }
     }*/
}
